# Python — do nauczenia

---

## Użyte

- `try`/`except`
- Docstringi
- Przekazywanie funkcji jako wartości
- `json`
- Walrus operator `:=`
- `is` vs `==`
- Ternary
- Metody stringów (`strip`, `split`)
- Generator expressions i `next()`
- `dataclasses.asdict`
- `**dict` do konstruktora
- Truthiness
- Comprehensions zbiorowe

---

## Do nauczenia

1. `else`/`finally`
2. Pydantic
3. FastAPI
4. Unpacking z `*rest`
5. `break` / `continue`
6. Metody `dict` (`update`, `setdefault`)
7. Metody `list` (`sort`, `reverse`, `index`, `insert`, `copy`)
8. `map()` / `filter()`
9. `zip()`
10. Comprehensions słownikowe
11. `@property`
12. Operacje na `set` (`union`, `intersection`, `difference`)
13. `@staticmethod`
14. Magic methods (`__eq__`, `__hash__`, `__len__`, `__iter__`, `__contains__`)
15. `yield` jako generator
16. `copy` vs `deepcopy`
17. `itertools`, `functools.lru_cache`
18. Generyki (PEP 695 — `def foo[T](...)`, `class Foo[T]:`)
19. `match/case`, `re`, `os`/`sys`, `pathlib.Path`
20. `{value!r}` we f-stringu, multiline strings (`"""`)

---

## Dla chętnych

- `Protocol` / `ABC` / `@abstractmethod`
- `namedtuple`
- Closures i `nonlocal`
- Operator overloading
- `bytes` i encoding
- `concurrent.futures`
- `__all__` w `__init__.py`
- `csv`
- Dekoratory
- Deskryptory atrybutów
- Metaklasy
